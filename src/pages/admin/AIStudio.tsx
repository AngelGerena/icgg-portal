/**
 * Deprecated — AI Studio was replaced by Contra Corriente (BlogStudio).
 *
 * This file only exists so that an older copy left behind in the repo gets
 * overwritten rather than breaking the build. It imports nothing that has
 * been removed. Safe to delete from GitHub whenever convenient; nothing
 * routes to it.
 */
export { BlogStudio as AIStudio } from './BlogStudio';
